// module is for the User Class
class User {
    constructor(userID, availability) {
        this.userID = userID;
        this.availability = availability; // array of availability objects
    }
}

export default User;
